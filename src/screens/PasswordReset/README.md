# Password reset system

* User types email in `EmailScreen.js`
* User receives an email with an OTP code
* User types OTP in `OTPScreen.js`
* Server check if OTP is valid
* User types new password in `ChangeScreen.js`
* Server checks if new password is ok
